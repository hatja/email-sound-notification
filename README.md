# email-sound-notification
A node.js app that plays music when you get a new email.

npm install

Rename config.example.js to config.js and set the variables.

Change the .mp3 in imap.js

node app.js
